# Prometheus scrape config for the ValidatorForge metrics stack.
# Enable with: docker compose --profile monitoring up
global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: validatorforge
    metrics_path: /metrics
    static_configs:
      - targets: ["node:8080"]
